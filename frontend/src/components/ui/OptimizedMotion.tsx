'use client';\n\nimport { motion, AnimationProps, HTMLMotionProps } from 'framer-motion';\nimport { forwardRef, ReactNode } from 'react';\n\n// Optimized animation variants for common patterns\nexport const fadeInUp = {\n  initial: { opacity: 0, y: 20 },\n  animate: { opacity: 1, y: 0 },\n  exit: { opacity: 0, y: -20 },\n};\n\nexport const fadeIn = {\n  initial: { opacity: 0 },\n  animate: { opacity: 1 },\n  exit: { opacity: 0 },\n};\n\nexport const scaleIn = {\n  initial: { opacity: 0, scale: 0.8 },\n  animate: { opacity: 1, scale: 1 },\n  exit: { opacity: 0, scale: 0.8 },\n};\n\nexport const slideInLeft = {\n  initial: { opacity: 0, x: -20 },\n  animate: { opacity: 1, x: 0 },\n  exit: { opacity: 0, x: -20 },\n};\n\nexport const slideInRight = {\n  initial: { opacity: 0, x: 20 },\n  animate: { opacity: 1, x: 0 },\n  exit: { opacity: 0, x: 20 },\n};\n\n// Optimized transition presets for 60fps performance\nexport const fastTransition = {\n  duration: 0.2,\n  ease: [0.4, 0, 0.2, 1], // cubic-bezier for smooth animation\n};\n\nexport const normalTransition = {\n  duration: 0.3,\n  ease: [0.4, 0, 0.2, 1],\n};\n\nexport const slowTransition = {\n  duration: 0.4,\n  ease: [0.4, 0, 0.2, 1],\n};\n\n// Stagger animation for lists\nexport const staggerContainer = {\n  animate: {\n    transition: {\n      staggerChildren: 0.05, // Reduced stagger for faster perception\n    },\n  },\n};\n\nexport const staggerItem = {\n  initial: { opacity: 0, y: 20 },\n  animate: { \n    opacity: 1, \n    y: 0,\n    transition: fastTransition,\n  },\n};\n\n// Performance-optimized motion components\ninterface OptimizedMotionProps extends HTMLMotionProps<'div'> {\n  children: ReactNode;\n}\n\n// Pre-configured motion components for common use cases\nexport const FadeInDiv = forwardRef<HTMLDivElement, OptimizedMotionProps>(\n  ({ children, ...props }, ref) => (\n    <motion.div\n      ref={ref}\n      {...fadeIn}\n      transition={fastTransition}\n      {...props}\n    >\n      {children}\n    </motion.div>\n  )\n);\nFadeInDiv.displayName = 'FadeInDiv';\n\nexport const FadeInUpDiv = forwardRef<HTMLDivElement, OptimizedMotionProps>(\n  ({ children, ...props }, ref) => (\n    <motion.div\n      ref={ref}\n      {...fadeInUp}\n      transition={normalTransition}\n      {...props}\n    >\n      {children}\n    </motion.div>\n  )\n);\nFadeInUpDiv.displayName = 'FadeInUpDiv';\n\nexport const ScaleInDiv = forwardRef<HTMLDivElement, OptimizedMotionProps>(\n  ({ children, ...props }, ref) => (\n    <motion.div\n      ref={ref}\n      {...scaleIn}\n      transition={{\n        ...fastTransition,\n        type: 'spring',\n        stiffness: 300,\n        damping: 30,\n      }}\n      {...props}\n    >\n      {children}\n    </motion.div>\n  )\n);\nScaleInDiv.displayName = 'ScaleInDiv';\n\n// Optimized hover animations with reduced repaints\nexport const hoverScale = {\n  scale: 1.02,\n  transition: {\n    duration: 0.15,\n    ease: 'easeOut',\n  },\n};\n\nexport const hoverLift = {\n  y: -2,\n  boxShadow: '0 8px 25px rgba(0, 0, 0, 0.15)',\n  transition: {\n    duration: 0.2,\n    ease: 'easeOut',\n  },\n};\n\n// Tap animation for buttons\nexport const tapScale = {\n  scale: 0.98,\n  transition: {\n    duration: 0.1,\n    ease: 'easeInOut',\n  },\n};\n\n// Loading animation with reduced CPU usage\nexport const pulseAnimation = {\n  animate: {\n    opacity: [0.4, 1, 0.4],\n    transition: {\n      duration: 1.5,\n      repeat: Infinity,\n      ease: 'easeInOut',\n    },\n  },\n};\n\n// Optimized entrance animation for modals/overlays\nexport const modalVariants = {\n  hidden: { \n    opacity: 0, \n    scale: 0.95,\n    transition: {\n      duration: 0.15,\n    },\n  },\n  visible: { \n    opacity: 1, \n    scale: 1,\n    transition: {\n      duration: 0.2,\n      ease: [0.4, 0, 0.2, 1],\n    },\n  },\n};\n\nexport const overlayVariants = {\n  hidden: { opacity: 0 },\n  visible: { \n    opacity: 1,\n    transition: {\n      duration: 0.2,\n    },\n  },\n};\n\n// Performance monitoring hook for animations\nexport const usePerformanceOptimized = () => {\n  return {\n    // Reduce animations on slower devices\n    shouldReduceMotion: typeof window !== 'undefined' && \n      window.matchMedia('(prefers-reduced-motion: reduce)').matches,\n    \n    // Use transform3d for hardware acceleration\n    optimizedStyle: {\n      willChange: 'transform, opacity',\n      transform: 'translateZ(0)',\n      backfaceVisibility: 'hidden' as const,\n    },\n  };\n};